<div class="container">
    <div class="row">
        {% for product in products %}
            {% include get_cover_template('common/post/block') with {
                post: product,
                loop: loop,
                heading_element: 'h3'
            } %}
        {% endfor %}
    </div>
</div>
