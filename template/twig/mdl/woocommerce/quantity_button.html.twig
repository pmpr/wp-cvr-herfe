<div class="input-group-{{ prepend ? 'prepend' : 'append' }}">
    <button class="btn btn-secondary btn-{{ increase ? 'increase' : 'decrease' }}" type="button">
        {{ icon|raw }}
    </button>
</div>
