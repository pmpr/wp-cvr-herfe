{% set has_link = has_link|default(1) %}
{% if has_link %}
    <a href="{{ home_url()|esc_attr }}" rel="nofollow" class="{{ container_class|esc_attr }}">
{% endif %}
        {{ pmpr_do_action('render_site_logo', {
            key: logo_key,
            type: logo_type,
            size: logo_size|default([48, 48])
        }) }}
{% if has_link %}
    </a>
{% endif %}

