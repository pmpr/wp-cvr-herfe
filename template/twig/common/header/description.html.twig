{% if more_less is defined and more_less %}
    {% include get_cover_template('common/more_less') with {
        content: description,
    } %}
{% else %}
    {{ wpautop(description)|raw }}
{% endif %}

